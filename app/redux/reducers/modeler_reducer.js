const INITIALSTATE = {
  tree: [],
  isError: false,
  errorMessage: ''
};

const modelerReducer = (state = INITIALSTATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default modelerReducer;
